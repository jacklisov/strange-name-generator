# Strange Name Generator

A strange name generator generates various nicknames and texts from syllables and letters embedded in two dictionaries (vowels and consonants).

To generate a nickname, you should use `` generate(number of syllables)``, and to generate text, you should use ``gentext (number of sentences)``.
